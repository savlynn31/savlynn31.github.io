$(document).ready(function() {

	/****************
		Slick
	****************/

	$('.HTML').slick({
		infinite: true,
		slidesToShow: 3,
		speed: 100
	});

	$('.css').slick({
		infinite: true,
		slidesToShow: 3,
		speed: 100
	});

	$('.js').slick({
		infinite: true,
		slidesToShow: 3,
		speed: 100
	});

	$('.create').slick({
		infinite: true,
		slidesToShow: 3,
		speed: 100
	});

	$('.work').slick({
		infinite: true,
		slidesToShow: 3,
		speed: 100
	});
		

});
