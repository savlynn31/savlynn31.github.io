$(document).ready(function() {

	/****************
		Slick
	****************/

	$('.HTML').slick({
		infinite: true,
		slidesToShow: 1,
		speed: 100
	});

	$('.css').slick({
		infinite: true,
		slidesToShow: 1,
		speed: 100
	});

	$('.js').slick({
		infinite: true,
		slidesToShow: 1,
		speed: 100
	});
		
});
